"""Filesystem-related constants for Portfolio OS."""

DEFAULT_ENCODING: str = "utf-8"

# Default directory names
DIR_PROJECTS: str = "projects"
DIR_TEMPLATES: str = "templates"
DIR_META: str = ".porto"

# Default file names
FILE_PROJECT_INDEX: str = "index.json"
